module.exports = {
	name: 'reaction_added',
	async execute({ event, client }) {
		// try {
		// 	await client.chat.postMessage({
		// 		channel: event.item.channel,
		// 		text: 'Reaction added',
		// 	});
		// }
		// catch (error) {
		// 	console.error(error);
		// }
	},
};